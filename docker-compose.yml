version: '3.8'

services:
  kindmap-app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - PORT=${PORT}
      - DB_URL=${DB_URL}
      - DB_USERNAME=${DB_USERNAME}
      - DB_PASSWORD=${DB_PASSWORD}
    env_file:
      - .env